<h1> <%= @post.title %> </h1>
<p>
	<%= @post.body %>
</p>
<hr>
<%= link_to 'Редактировать', edit_post_path(@post), :class => 'btn btn-warning'%>
<%= link_to 'Удалить пост', post_path(@post), data: { "turbo-method": :delete }, class: 'btn btn-danger', onclick: "return confirm('Вы уверены?')" %>
